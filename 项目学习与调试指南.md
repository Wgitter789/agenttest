# LangChain Agent 示例项目：从零理解、运行与调试

> 适用读者：第一次接触 LangChain、Agent、RAG 和向量数据库的学习者。  
> 对应项目：`AI大模型RAG与智能体开发_Agent项目`  
> 文档依据：当前项目中的实际代码与配置，而不是泛泛介绍。  
> 建议读法：先读第 1～4 章建立全局认识，再照第 5 章运行；遇到问题查第 10～12 章。

---

## 1. 先用一句话理解这个项目

这是一个“扫地机器人智能客服”：用户在网页中提问，大模型判断应该直接回答还是调用工具；工具可以查本地知识库、读取模拟用户数据、获取模拟天气；中间件会记录执行日志，并在“生成个人使用报告”时动态切换系统提示词。

它同时演示了四类能力：

1. **聊天界面**：Streamlit 提供网页聊天框。
2. **Agent 决策**：LangChain `create_agent` 让大模型自主选择工具并循环执行。
3. **RAG 问答**：Chroma 从本地资料中找相关片段，再由大模型归纳答案。
4. **运行控制**：Middleware（中间件）监控工具、记录模型调用、动态切换提示词。

这个项目适合学习结构与流程，但它仍是教学 Demo，并非可以直接上线的生产系统。天气、用户 ID、月份和位置都是模拟数据，且目前没有真正的多轮对话记忆。

---

## 2. 开始前必须认识的概念

### 2.1 大模型（LLM）与聊天模型（Chat Model）

大模型负责理解自然语言、做决策、组织回答。聊天模型接收的不是单个字符串，而是一组带角色的消息，例如：

```text
system: 你是扫地机器人客服
user: 机器总是被电线缠住怎么办？
assistant: ……
```

本项目使用 `ChatTongyi` 调用阿里云百炼/DashScope 上的 `qwen3-max`。模型名称在 `config/rag.yml` 中配置。

### 2.2 Agent（智能体）

普通聊天调用通常是“问题 → 模型 → 答案”。Agent 则是一个循环：

```text
收到问题
  ↓
模型判断下一步
  ├─ 信息够了 → 给最终答案
  └─ 信息不够 → 选择工具并生成参数
                    ↓
                 执行工具
                    ↓
              把工具结果交还模型
                    ↓
                 再次判断
```

因此，Agent 不是另一个模型。它是“模型 + 工具 + 提示词 + 循环控制 + 状态”的组合。

### 2.3 ReAct

ReAct 可以理解为 Reason（判断/推理）与 Act（行动/调用工具）交替进行。这个项目的主提示词要求模型遵循类似流程。

要特别注意：真正的工程中，不应要求模型把完整的内部思维过程原样展示给用户。更合适的做法是只展示简短的行动说明，例如“我正在查询维护资料”，而把详细执行轨迹放进日志或可观测平台。

### 2.4 Tool（工具）

工具就是允许模型调用的 Python 函数。普通函数加上 `@tool` 后，LangChain 会把函数名、说明和参数结构告诉模型。

例如：

```python
@tool(description="获取指定城市的天气，以消息字符串的形式返回")
def get_weather(city: str) -> str:
    return f"城市{city}天气为晴天……"
```

模型不会读取函数源代码来理解工具，主要依赖以下信息：

- 工具名 `get_weather`
- `description` 中写的用途
- 参数名 `city`
- 参数类型 `str`

所以工具说明和参数命名会直接影响 Agent 是否能正确选择、正确传参。

### 2.5 Middleware（中间件）

中间件是在 Agent 执行过程的特定位置插入的逻辑。可以把它想成高速公路上的检查站：不改变车辆的主要目的地，但可以记录、检查、修改或拦截过程。

本项目有三种中间件：

- `wrap_tool_call`：包住每一次工具调用，可在执行前后记录信息或处理异常。
- `before_model`：每次调用大模型前执行。
- `dynamic_prompt`：每次调用大模型前动态决定使用哪份系统提示词。

`create_agent` 返回的 Agent 底层由 LangGraph 执行；中间件钩子也运行在这个执行图中。

### 2.6 State 与 Runtime Context

这两个概念容易混淆：

- **State（状态）**：Agent 流程中的消息等数据，例如用户消息、AI 工具请求、工具返回值和最终回复。
- **Runtime Context（运行时上下文）**：本次执行附带的额外控制信息。本项目传入 `{"report": False}`，当报告工具被调用后改成 `True`，用它切换提示词。

本项目每次 `execute_stream()` 都会创建新的 context，所以 `report` 标记只在当前这一次请求内有效，不会永久污染后续请求。

### 2.7 RAG

RAG 是 Retrieval-Augmented Generation，即“检索增强生成”。它解决的问题是：模型训练时不一定见过你的私有资料，即使见过也可能记不准。

本项目的 RAG 流程是：

```text
知识库建设（通常只在资料变化时执行）
TXT/PDF → Document → 文本分片 → Embedding 向量 → Chroma

用户查询（每次知识类问题都可能执行）
问题 → Embedding 向量 → Chroma 相似度检索 → 取最相关 3 个片段
     → 填入 RAG 提示词 → qwen3-max 总结 → 返回 Agent
```

RAG 本身不是 Agent。这里的 `rag_summarize` 只是 Agent 可以选择的一个工具。

### 2.8 Embedding（嵌入向量）

Embedding 模型把文字变成一串数字。语义相近的文本，其数字向量在数学空间中也更接近。

本项目使用 `text-embedding-v4`：

- 建库时：把知识片段转成向量。
- 查询时：把用户问题也转成向量。
- Chroma：比较问题向量与资料向量的相似度。

Embedding 模型和聊天模型不是同一种用途：前者负责“找相近内容”，后者负责“理解、决策和写回答”。

### 2.9 Vector Store 与 Retriever

- **Vector Store（向量库）**：保存文本、向量和元数据。本项目用本地 Chroma。
- **Retriever（检索器）**：向量库外面的一层统一检索接口。输入查询，返回若干 `Document`。

本项目 `k: 3`，表示默认取相似度最高的 3 个片段，不表示只存 3 个片段。

### 2.10 Prompt（提示词）

提示词不是用户在聊天框中输入的那一句话。项目里至少有三类输入：

- 系统提示词：规定角色、工具边界和执行规则。
- 用户消息：用户真正提出的问题。
- 工具结果：程序查到的知识或数据，作为后续模型调用的新消息。

### 2.11 Chain（链）与 LCEL 管道

`rag/rag_service.py` 中的：

```python
self.prompt_template | print_prompt | self.model | StrOutputParser()
```

可以读作：

1. 用变量生成完整 Prompt；
2. 打印 Prompt，方便教学调试；
3. 交给聊天模型；
4. 把模型消息转成普通字符串。

竖线 `|` 是 LangChain Expression Language（LCEL）的管道组合语法，不是 Python 的普通数据管道。

---

## 3. 项目总体架构

### 3.1 组件关系图

```text
┌──────────────────────────────────────────────────────────────┐
│ app.py：Streamlit 网页                                       │
│ 接收输入、显示历史、展示流式回复                              │
└───────────────────────────┬──────────────────────────────────┘
                            │ execute_stream(query)
                            ▼
┌──────────────────────────────────────────────────────────────┐
│ agent/react_agent.py：ReactAgent                              │
│ create_agent = 模型 + 系统提示词 + 7 个工具 + 3 个中间件      │
└──────────────┬───────────────────────┬───────────────────────┘
               │                       │
               │ 调模型                │ 调工具
               ▼                       ▼
┌────────────────────────┐   ┌─────────────────────────────────┐
│ model/factory.py       │   │ agent/tools/agent_tools.py      │
│ ChatTongyi             │   │ RAG、天气、位置、用户、报告数据 │
│ DashScopeEmbeddings    │   └───────────────┬─────────────────┘
└────────────────────────┘                   │ rag_summarize
                                             ▼
                               ┌───────────────────────────────┐
                               │ rag/rag_service.py            │
                               │ 检索 + 拼上下文 + 模型总结     │
                               └──────────────┬────────────────┘
                                              ▼
                               ┌───────────────────────────────┐
                               │ rag/vector_store.py           │
                               │ 文档加载、切分、Chroma 检索    │
                               └──────────────┬────────────────┘
                                              ▼
                              data/ + chroma_db/ + md5.text

横切整个 Agent 流程：agent/tools/middleware.py
全局基础设施：config/、prompts/、utils/、logs/
```

### 3.2 一次普通知识问答的时序

以“机器人为什么总在沙发下迷路？”为例：

1. `app.py` 收到输入。
2. `ReactAgent.execute_stream()` 构造用户消息，并把 `report` 上下文设为 `False`。
3. `log_before_model` 记录即将调用模型。
4. `report_prompt_switch` 返回普通客服提示词。
5. 模型判断需要专业资料，产生 `rag_summarize(query=...)` 工具请求。
6. `monitor_tool` 记录工具名和参数。
7. `RagSummarizeService` 把问题交给 Retriever。
8. Embedding 模型把问题转成向量，Chroma 返回 3 个相关片段。
9. RAG 链把问题和片段放入 `rag_summarize.txt`，再调用聊天模型归纳。
10. 归纳结果作为工具消息返回 Agent。
11. Agent 再调用一次聊天模型，生成面向用户的最终回答。
12. Streamlit 逐字显示结果，并把它加入当前浏览器会话的显示记录。

这里可能发生两次聊天模型调用：一次在 RAG 工具内部做资料总结，一次在 Agent 外层组织最终答复；还会发生一次 Embedding API 调用。理解这一点有助于判断延迟与费用。

### 3.3 一次“生成报告”的特殊时序

主提示词要求按以下顺序执行：

```text
get_user_id
  ↓
get_current_month（用户没指定月份时）
  ↓
fill_context_for_report
  ↓ monitor_tool 将 runtime.context["report"] 改为 True
  ↓
下一轮模型调用时，dynamic_prompt 切换为 report_prompt.txt
  ↓
fetch_external_data(user_id, month)
  ↓
模型生成 Markdown 报告
```

`fill_context_for_report` 的返回文字本身并不重要。真正起作用的是 `monitor_tool` 识别到这个工具名后修改运行时上下文。

---

## 4. 目录与每个组件的职责

### 4.1 业务文件总览

```text
项目根目录/
├─ app.py                         # Streamlit 网页入口
├─ agent/
│  ├─ react_agent.py             # 组装并运行 Agent
│  └─ tools/
│     ├─ agent_tools.py           # 7 个工具
│     └─ middleware.py            # 3 个中间件
├─ model/
│  └─ factory.py                 # 聊天模型与 Embedding 模型工厂
├─ rag/
│  ├─ rag_service.py             # RAG 检索总结服务
│  └─ vector_store.py            # 文档入库与向量检索
├─ utils/
│  ├─ config_handler.py          # 加载 YAML 配置
│  ├─ file_handler.py            # TXT/PDF/MD5 文件操作
│  ├─ logger_handler.py          # 控制台与文件日志
│  ├─ path_tool.py               # 计算项目绝对路径
│  └─ prompt_loader.py           # 读取三份提示词
├─ config/
│  ├─ agent.yml                  # 外部记录文件路径
│  ├─ chroma.yml                 # 向量库与分片参数
│  ├─ prompts.yml                # 提示词文件路径
│  └─ rag.yml                    # 模型名称
├─ prompts/
│  ├─ main_prompt.txt            # 普通客服系统提示词
│  ├─ rag_summarize.txt          # RAG 内部总结提示词
│  └─ report_prompt.txt          # 报告场景系统提示词
├─ data/
│  ├─ *.txt / *.pdf              # RAG 原始知识资料
│  └─ external/records.csv       # 模拟用户月度记录
├─ chroma_db/                    # 根目录下的本地向量数据库
├─ md5.text                      # 已入库原始文件的 MD5
├─ logs/                         # 每日运行日志
├─ .idea/                        # PyCharm 工程设置，不是业务代码
└─ __pycache__/                  # Python 字节码缓存，可忽略
```

项目中还出现了 `agent/chroma_db/` 和 `rag/chroma_db/`。它们不是三套业务设计，而很可能是曾经从不同工作目录启动程序造成的重复数据库，原因见第 11.2 节。

### 4.2 `app.py`：网页入口

主要职责：

- 创建标题与聊天输入框。
- 在 `st.session_state["agent"]` 中保存一个 `ReactAgent` 实例，避免每次页面重跑都重新创建。
- 在 `st.session_state["message"]` 中保存用于页面展示的历史消息。
- 调用 `execute_stream()` 并逐字输出。

Streamlit 有一个重要特性：用户每次交互时，脚本通常会从上到下重新执行。`session_state` 用来在重跑之间保留数据。

`capture()` 做了两件事：

1. 把 Agent 产生的每个文本块保存到 `response_messages`。
2. 把文本块拆成字符，每个字符等待 0.01 秒再显示，制造“打字机”效果。

这里的“流式”并不等同于模型原生 token 流。Agent 使用 `stream_mode="values"` 先按状态步骤产生内容，界面又人为按字符切分。

重要限制：`message` 历史只用于显示。调用 Agent 时只传了最新的 `prompt`，没有把此前消息一起传入，所以刷新前虽然能看到聊天记录，模型却无法利用上一轮信息。

### 4.3 `agent/react_agent.py`：Agent 组装器

`ReactAgent.__init__()` 调用 `create_agent()`，组合四个部分：

| 参数 | 本项目传入内容 | 作用 |
|---|---|---|
| `model` | `chat_model` | 负责判断、选工具和生成答案 |
| `system_prompt` | `load_system_prompts()` | 默认客服规则 |
| `tools` | 7 个工具 | 扩展模型可访问的能力 |
| `middleware` | 3 个中间件 | 监控与动态控制流程 |

`execute_stream(query)` 的输入结构是：

```python
{
    "messages": [
        {"role": "user", "content": query}
    ]
}
```

之后：

```python
self.agent.stream(
    input_dict,
    stream_mode="values",
    context={"report": False},
)
```

- `stream_mode="values"`：每一步返回当前完整状态的值。
- `chunk["messages"][-1]`：取当前状态中最后一条消息。
- 仅在 `latest_message.content` 非空时向 UI 输出。
- `context={"report": False}`：为动态报告提示词准备开关。

文件底部的 `if __name__ == '__main__':` 是独立调试入口，可以不启动网页，直接测试 Agent。

### 4.4 `agent/tools/agent_tools.py`：7 个工具

#### `rag_summarize(query)`

从向量库检索资料，并让 RAG 内部模型总结。适用于使用说明、故障、维护、选购等知识问题。

模块加载时就会执行：

```python
rag = RagSummarizeService()
```

这意味着只要导入这个工具模块，就会初始化 RAG、向量库和模型，不是等第一次调用工具时才初始化。

#### `get_weather(city)`

返回固定格式的模拟晴天天气。它没有调用天气 API，所以“实时天气”只存在于描述中，实际不是实时数据。

#### `get_user_location()`

随机返回深圳、合肥或杭州。它没有读取 IP、GPS 或用户资料。

#### `get_user_id()`

随机返回 `1001`～`1010`。同一个用户多次询问，结果可能不同。

#### `get_current_month()`

随机返回 2025 年的某个月，并不读取系统日期。函数名与工具说明会让模型以为它是真实当前月份，这是教学模拟，需要特别留意。

#### `fetch_external_data(user_id, month)`

第一次调用时读取 `data/external/records.csv`，把数据缓存到模块级字典 `external_data`，结构大致是：

```python
{
    "1001": {
        "2025-01": {
            "特征": "……",
            "效率": "……",
            "耗材": "……",
            "对比": "……",
        }
    }
}
```

后续调用直接查内存，不会重复读取 CSV。找不到用户或月份时记一条 warning 并返回空字符串。

源码标注返回类型是 `str`，实际成功时返回字典。这通常仍可被工具框架序列化，但类型声明不准确，正式项目应统一。

#### `fill_context_for_report()`

这是一个“信号工具”。函数只返回说明文字；`monitor_tool` 看见它被调用后，会把 `report` 标志改为 `True`。它演示了“工具调用触发运行上下文变化”的设计。

### 4.5 `agent/tools/middleware.py`：3 个中间件

#### `monitor_tool`

执行顺序：

1. 记录工具名。
2. 记录工具参数。
3. 调用 `handler(request)` 执行真正工具。
4. 成功后记录日志。
5. 如果工具名为 `fill_context_for_report`，设置报告标志。
6. 如果失败，记录错误并重新抛出异常。

它目前只“记录后再抛出”，没有把错误转换成模型可理解的友好 `ToolMessage`，所以工具异常可能直接中断整次请求。

#### `log_before_model`

每次模型调用前输出：

- 当前状态有多少条消息（INFO）。
- 最后一条消息的类型和内容（DEBUG，写入日志文件）。

因为 Agent 可能进行多轮“模型 → 工具 → 模型”，一次用户请求里这个中间件可能执行多次。

#### `report_prompt_switch`

每次生成系统提示词时读取：

```python
request.runtime.context.get("report", False)
```

- `False`：使用 `main_prompt.txt`。
- `True`：使用 `report_prompt.txt`。

这不是替换用户消息，而是替换模型在当前步骤看到的系统级规则。

### 4.6 `model/factory.py`：模型工厂

定义了一个抽象工厂接口和两个实现：

- `ChatModelFactory` → `ChatTongyi(model="qwen3-max")`
- `EmbeddingsFactory` → `DashScopeEmbeddings(model="text-embedding-v4")`

文件最后立即创建两个全局对象：

```python
chat_model = ChatModelFactory().generator()
embed_model = EmbeddingsFactory().generator()
```

好处是全项目复用同一个配置入口；代价是导入阶段就进行初始化，环境变量或依赖错误会很早出现。

这里的“工厂模式”教学意义大于实际必要性：如果将来要按环境选择不同厂商、模型或参数，工厂能统一封装；目前只有一种实现，直接实例化也能工作。

### 4.7 `rag/vector_store.py`：知识入库与检索

构造函数创建：

1. Chroma 向量库。
2. `RecursiveCharacterTextSplitter` 文本切分器。

`get_retriever()` 把向量库变成检索器，设置 `k=3`。

`load_document()` 的入库过程：

1. 在 `data/` 找后缀为 txt 或 pdf 的文件。
2. 为每个文件计算 MD5。
3. 如果 MD5 已在 `md5.text`，认为已处理并跳过。
4. 根据后缀调用 TXT 或 PDF Loader。
5. 按配置切成重叠文本片段。
6. 调用 Embedding API 并把片段写入 Chroma。
7. 把文件 MD5 追加到 `md5.text`。

为什么要 `chunk_overlap: 20`：一个完整信息可能刚好横跨两个片段边界，重叠少量字符可以减少语义被切断的概率。

为什么要 MD5：避免每次启动都把同一个文件重复写入库。但当前实现只解决“完全相同的文件重复导入”，不能自动删除旧版本产生的向量。

### 4.8 `rag/rag_service.py`：RAG 总结服务

初始化时取得 Retriever、加载 RAG 提示词、创建模板并组装 Chain。

`retriever_docs(query)` 只负责检索，适合单独调试检索质量。

`rag_summarize(query)`：

1. 检索文档。
2. 为每个文档增加“参考资料 1/2/3”标签，并保留 metadata。
3. 把 `{input}` 和 `{context}` 填入提示词。
4. 调用聊天模型总结。
5. 返回普通字符串。

`print_prompt()` 会把完整的 RAG Prompt 打到终端。它非常适合教学，但资料可能包含敏感信息，生产环境中不宜无条件打印。

### 4.9 `utils/`：基础工具

#### `utils/config_handler.py`

使用 PyYAML 加载四份 YAML。文件被导入时立即读取配置，并生成：

```python
rag_conf
chroma_conf
prompts_conf
agent_conf
```

因此 YAML 缺失、缩进错误或路径错误会在程序启动早期报错。

#### `utils/file_handler.py`

- `get_file_md5_hex()`：以 4 KB 块读取文件并计算 MD5，避免一次把大文件读进内存。
- `listdir_with_allowed_type()`：筛选允许后缀的文件。
- `pdf_loader()`：通过 `PyPDFLoader` 读取 PDF。
- `txt_loader()`：明确用 UTF-8 读取 TXT。

历史日志中曾出现按系统默认 GBK 读取 UTF-8 文件导致的 `UnicodeDecodeError`；当前源码已经在 `TextLoader` 中显式指定 `encoding="utf-8"`，这是正确修复方向。

#### `utils/logger_handler.py`

创建名为 `agent` 的 logger：

- INFO 及以上显示在终端。
- DEBUG 及以上写入 `logs/agent_YYYYMMDD.log`。
- 每行包括时间、logger 名、级别、文件、行号和消息。
- 如果 logger 已有 handler 就直接复用，避免重复输出。

#### `utils/path_tool.py`

根据 `utils/path_tool.py` 自身位置向上两级找到项目根目录，再把相对路径拼成绝对路径。它让大部分配置文件、提示词和数据路径不依赖启动目录。

#### `utils/prompt_loader.py`

先从 `config/prompts.yml` 取得路径，再读取三份提示词。若配置键或文件有问题，会记录日志并继续抛出异常，让调用方看到失败。

### 4.10 `config/`：可调整参数

#### `config/rag.yml`

| 配置 | 当前值 | 含义 |
|---|---|---|
| `chat_model_name` | `qwen3-max` | Agent 与 RAG 总结使用的聊天模型 |
| `embedding_model_name` | `text-embedding-v4` | 建库与查询使用的向量模型 |

如果更换 Embedding 模型，旧向量库通常应重新构建，不能默认与新模型混用。

#### `config/chroma.yml`

| 配置 | 当前值 | 含义 |
|---|---:|---|
| `collection_name` | `agent` | Chroma 集合名 |
| `persist_directory` | `chroma_db` | 本地库目录；当前是相对路径 |
| `k` | `3` | 每次检索返回的片段数 |
| `data_path` | `data` | 原始知识资料目录 |
| `md5_hex_store` | `md5.text` | 已入库文件指纹记录 |
| `allow_knowledge_file_type` | txt、pdf | 可导入类型 |
| `chunk_size` | `200` | 每片最多约 200 个 Python 字符 |
| `chunk_overlap` | `20` | 相邻片段重叠约 20 个字符 |
| `separators` | 多种中英文符号 | 切分时依次尝试的边界 |

中文字符串的 `len()` 通常按 Unicode 字符计数，并不等于模型 token 数量。

#### `config/prompts.yml`

只负责把三种业务用途映射到三个提示词文件。

#### `config/agent.yml`

指定模拟外部数据 `data/external/records.csv` 的位置。

### 4.11 `prompts/`：模型行为规则

#### `prompts/main_prompt.txt`

普通客服主规则，详细描述了工具用途，并对个人报告规定固定顺序。

工具说明同时出现在 `@tool(description=...)` 和主提示词里，存在重复。重复有助于教学观察，但两处日后若修改不一致，模型可能困惑。

#### `prompts/rag_summarize.txt`

有两个模板变量：

- `{input}`：用户查询。
- `{context}`：检索到的 3 个文档片段。

它要求回答严格基于资料、不扩大范围，并仅输出中文纯文本。

#### `prompts/report_prompt.txt`

报告模式下使用，要求生成指定标题的中文 Markdown 报告，并根据外部数据给建议。

### 4.12 `data/`、`chroma_db/`、`md5.text` 与 `logs/`

- `data/*.txt`、`data/*.pdf`：知识源，即 RAG 的“教材”。
- `data/external/records.csv`：结构化模拟业务数据，不进入向量库。
- `chroma_db/`：加工后的可检索向量数据，可以从源资料重新生成。
- `md5.text`：记录哪些源文件版本已经加工过。
- `logs/`：观察 Agent 工具选择、参数和异常的第一现场。

不要把“原始资料”和“向量库”当成同一份东西。向量库是从原始资料计算出来的派生数据。

---

## 5. 从零运行项目（Windows PowerShell）

### 5.1 前置条件

建议准备：

- Python 3.10 或 3.11。
- 可正常访问阿里云百炼/DashScope API 的网络。
- 有效的 DashScope API Key，且账户可调用配置中的两个模型。
- PowerShell 或 PyCharm 终端。

源码使用了 `list[str]` 和 `A | B` 类型语法，因此不要使用 Python 3.9 或更旧版本。

### 5.2 进入项目根目录

```powershell
cd "C:\Users\Wuzeyu\Desktop\agent_test\AI大模型RAG与智能体开发_Agent项目"
```

后面的所有命令都应在这个目录执行。先运行：

```powershell
Get-Location
```

确认末尾是 `AI大模型RAG与智能体开发_Agent项目`。

### 5.3 创建并激活虚拟环境

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
```

如果 PowerShell 阻止激活脚本，可以只对当前窗口临时放宽：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
```

看到命令行前出现 `(.venv)`，说明已激活。

### 5.4 安装依赖

当前项目没有 `requirements.txt` 或 `pyproject.toml`，这是复现上的缺口。根据实际 import，至少需要：

```powershell
python -m pip install "langchain>=1,<2" langchain-community langchain-chroma langchain-text-splitters langgraph streamlit dashscope pyyaml pypdf
```

为什么要求 LangChain 1.x：当前代码使用 `langchain.agents.create_agent` 和新版 middleware API，这是 1.x 时代的 Agent 写法。不要照搬老教程中的 `initialize_agent`、`AgentExecutor` 代码来替换。

安装后立即保存本机确认可用的精确版本：

```powershell
python -m pip freeze > requirements-lock.txt
```

这份 lock 文件比只写宽泛版本范围更适合以后复现。若这是课程配套项目，优先使用课程提供者的原始依赖版本文件；本项目目录目前没有该文件，因此上面的安装清单是根据源码推导的最小集合。

### 5.5 配置 API Key

只对当前 PowerShell 窗口生效：

```powershell
$env:DASHSCOPE_API_KEY = "替换成你自己的Key"
```

检查是否配置，但不要打印 Key 本身：

```powershell
if ($env:DASHSCOPE_API_KEY) { "DASHSCOPE_API_KEY 已配置" } else { "尚未配置" }
```

安全提醒：

- 不要把真实 Key 写进 Python、YAML、截图、聊天记录或 Git 提交。
- 如果 Key 曾泄露，应在控制台撤销并重新生成。
- 新开终端后，当前窗口的临时环境变量会消失，需要重新设置；正式项目可使用安全的 Secret 管理方式。

### 5.6 做启动前检查

检查 Python 和关键模块：

```powershell
python --version
python -c "import langchain, streamlit, chromadb, dashscope, yaml; print('依赖导入成功')"
```

检查配置能否读取：

```powershell
python -c "from utils.config_handler import rag_conf, chroma_conf; print(rag_conf); print(chroma_conf)"
```

检查模型对象能否创建：

```powershell
python -c "from model.factory import chat_model, embed_model; print(type(chat_model).__name__); print(type(embed_model).__name__)"
```

### 5.7 首次构建或更新知识库

项目当前已经带有根目录 `chroma_db/` 和 `md5.text`，一般可以先直接检索。若从空项目开始、加入了新资料，或决定重建，则运行：

```powershell
python -m rag.vector_store
```

这个入口不仅入库，入库后还会用“迷路”做一次检索并打印结果。观察终端和当天日志：

- “内容加载成功”：已切分并写入。
- “内容已经存在知识库内，跳过”：MD5 已记录。
- “没有有效文本内容”：Loader 没读到内容。
- `UnicodeDecodeError`：文本编码与 Loader 设置不一致。
- 401/鉴权错误：API Key 无效或未配置。

入库会调用 Embedding API，资料越多，时间和费用越高。

### 5.8 分层测试，再启动网页

先测最小单元，能更快定位问题。

测试模拟天气工具：

```powershell
python -c "from agent.tools.agent_tools import get_weather; print(get_weather.invoke({'city':'杭州'}))"
```

测试向量检索：

```powershell
python -c "from rag.vector_store import VectorStoreService; docs=VectorStoreService().get_retriever().invoke('机器人迷路'); print(len(docs)); [print(d.page_content[:120]) for d in docs]"
```

测试完整 RAG：

```powershell
python -c "from rag.rag_service import RagSummarizeService; print(RagSummarizeService().rag_summarize('小户型适合怎样的扫地机器人'))"
```

测试完整 Agent：

```powershell
python -m agent.react_agent
```

最后启动网页：

```powershell
python -m streamlit run app.py
```

终端会显示本地地址，通常是 `http://localhost:8501`。停止服务按 `Ctrl+C`。

---

## 6. 怎样使用网页与设计测试问题

不要只问一种问题。按能力类型设计测试，更容易看懂 Agent。

### 6.1 不需要工具的简单对话

```text
你好，你能做什么？
```

预期：模型可能直接说明能力，不必调用工具。

### 6.2 RAG 知识问答

```text
扫地机器人总在沙发附近迷路，应该怎么处理？
```

预期：调用 `rag_summarize`；终端能看到完整 RAG Prompt，日志能看到工具名和参数。

### 6.3 多工具组合

```text
我所在城市今天的湿度适合使用拖地功能吗？
```

预期：可能先调用 `get_user_location`，再调用 `get_weather`，最后给建议。请记住这两个工具都是模拟的。

### 6.4 指定用户和月份生成报告

```text
请生成用户 1001 在 2025-01 的使用报告。
```

预期：不需要随机获取 ID 或月份，但按照主提示词仍应调用 `fill_context_for_report`，再调用 `fetch_external_data`，最后使用报告提示词写报告。

### 6.5 未指定用户和月份生成报告

```text
请生成我的本月使用报告。
```

预期：依次获取模拟用户 ID、模拟月份、填充报告上下文、读取数据。因为前两个结果随机，多次测试可能生成不同报告。

### 6.6 验证“没有多轮记忆”

第一句：

```text
我的机器人型号叫 A100，请记住。
```

第二句：

```text
我的机器人型号是什么？
```

页面仍显示第一句，但第二次 `execute_stream()` 只把第二句发给 Agent，所以模型无法可靠知道答案。若偶然答对，也不能证明存在记忆，应查看传入的 `input_dict`。

---

## 7. 调试方法：从外到内逐层定位

### 7.1 调试的基本原则

不要一上来只运行网页，然后面对长堆栈猜原因。按下面层次定位：

```text
Python/依赖
   ↓
配置与 API Key
   ↓
模型和 Embedding
   ↓
文件读取与 Chroma
   ↓
RAG 链
   ↓
单个 Tool
   ↓
Agent 循环与 Middleware
   ↓
Streamlit UI
```

底层没通，上层必然失败；底层逐层通过，问题范围会非常小。

### 7.2 最值得设置的断点

建议按一次完整请求的顺序设置：

1. `app.py`：`if prompt:` 内部，确认网页收到什么。
2. `agent/react_agent.py`：`self.agent.stream(...)` 前，检查输入消息和 context。
3. `agent/tools/middleware.py`：`log_before_model()`，查看每次模型调用前的消息。
4. `agent/tools/middleware.py`：`monitor_tool()` 的 `handler(request)` 前后，查看工具名、参数、返回值。
5. `agent/tools/agent_tools.py`：预期被调用的具体工具函数体。
6. `rag/rag_service.py`：`context_docs = self.retriever_docs(query)` 后，查看命中的 3 个文档。
7. `rag/rag_service.py`：`self.chain.invoke(...)` 前，检查最终 `{input}` 和 `{context}`。
8. `report_prompt_switch()`：查看 `is_report` 何时从 `False` 变为 `True`。

### 7.3 在 PyCharm 中调试普通模块

对 `rag.vector_store` 或 `agent.react_agent` 建立 Python 运行配置：

- Run kind：Module name。
- Module name：`rag.vector_store` 或 `agent.react_agent`。
- Working directory：项目根目录。
- Environment variables：配置 `DASHSCOPE_API_KEY`。
- Interpreter：选择项目 `.venv`。

用 Module name 比直接运行子目录中的 `.py` 文件更稳妥，可避免模块搜索路径差异。

### 7.4 在 PyCharm 中调试 Streamlit

建立 Python 运行配置：

- Module name：`streamlit`
- Parameters：`run app.py`
- Working directory：项目根目录
- Python interpreter：项目 `.venv`
- Environment variables：`DASHSCOPE_API_KEY=你的Key`

以 Debug 启动，在浏览器提交问题后，断点就会命中。

### 7.5 看懂 Agent 消息序列

典型工具调用会产生：

```text
HumanMessage        用户问题
AIMessage           模型提出工具调用（content 可能为空，tool_calls 有值）
ToolMessage         工具执行结果
AIMessage           模型最终回答
```

因此不能只看 `AIMessage.content` 判断模型是否“什么也没做”。当它准备调用工具时，正文可能为空，真正的信息在 `tool_calls`。

临时调试时，可以在流循环里观察：

```python
print(type(latest_message).__name__)
print(latest_message)
```

调试结束后应删除或降低敏感信息输出，避免把用户数据与完整 Prompt 写入终端或日志。

### 7.6 看懂日志

日志文件名形如：

```text
logs/agent_20260904.log
```

常见记录：

```text
[log_before_model]即将调用模型，带有1条消息。
[tool monitor]执行工具：rag_summarize
[tool monitor]传入参数：{'query': '机器人迷路'}
[tool monitor]工具rag_summarize调用成功
```

解读方法：

- 同一请求出现多条 `log_before_model` 是正常的，表示 Agent 在循环。
- 有“执行工具”却没有“调用成功”，紧接着有 ERROR，说明工具内部失败。
- 完全没有工具日志，说明模型没有选择工具，或在第一次模型调用前就失败。
- `fetch_external_data` warning 说明随机/指定的 ID 和月份组合在 CSV 中不存在。

### 7.7 只调检索，不调总结

如果最终答案不理想，先区分“没找到好资料”还是“找到资料但总结不好”。

```python
from rag.rag_service import RagSummarizeService

service = RagSummarizeService()
docs = service.retriever_docs("机器人被电线缠住")
for index, doc in enumerate(docs, start=1):
    print(index, doc.page_content, doc.metadata)
```

- 命中文档不相关：调整查询、分片、`k`、知识内容或 Embedding 模型。
- 命中文档相关但答案不佳：调整 `rag_summarize.txt` 或聊天模型。

### 7.8 验证提示词是否切换

在 `report_prompt_switch()` 临时查看：

```python
print("report mode:", is_report)
```

发普通问题时应一直是 `False`；发报告请求并调用 `fill_context_for_report` 后，后续模型调用应为 `True`。

如果一直为 `False`，检查：

1. 模型有没有真的调用 `fill_context_for_report`。
2. `monitor_tool` 是否执行成功。
3. 工具名是否精确匹配。
4. `context` 是否为可变字典，当前调用是否传入它。

---

## 8. 怎样新增或修改功能

### 8.1 新增一个简单工具

例如增加“查询保修期”的模拟工具。

第一步，在 `agent/tools/agent_tools.py` 定义：

```python
@tool(description="根据设备序列号查询剩余保修天数，以纯文本返回")
def get_warranty(serial_number: str) -> str:
    return f"设备 {serial_number} 剩余保修 180 天"
```

第二步，在 `agent/react_agent.py` 导入它，并加入 `tools=[...]`。

第三步，在主提示词中补充适用场景（如果希望规则更明确）。

第四步，先单独测试：

```powershell
python -c "from agent.tools.agent_tools import get_warranty; print(get_warranty.invoke({'serial_number':'SN001'}))"
```

第五步，发自然语言问题验证 Agent 是否会选择它。

工具设计建议：

- 一个工具只做一件清晰的事。
- 参数少而明确，名称不要含糊。
- 返回稳定、简洁、可序列化的数据。
- 描述清楚“何时用”和“何时不要用”。
- 外部写操作要考虑权限、确认、幂等和审计。

### 8.2 增加知识资料

1. 把 UTF-8 TXT 或可提取文字的 PDF 放进 `data/`。
2. 在项目根目录执行 `python -m rag.vector_store`。
3. 看日志确认“内容加载成功”。
4. 用 `retriever_docs()` 验证查询能否命中新资料。

扫描版 PDF 只有图片、没有文本层时，`PyPDFLoader` 通常无法直接取得有效正文，需要先 OCR。

### 8.3 修改已有知识资料

修改文件后 MD5 会变化，当前程序会把新片段追加到 Chroma，但旧版本片段不会自动删除。结果可能同时检索到新旧内容。

教学环境最简单的干净重建思路是：

1. 先停止所有正在使用 Chroma 的程序。
2. 备份原始 `data/`。
3. 删除正确的向量库目录及 `md5.text`。
4. 从项目根目录重新入库。

删除属于破坏性操作，务必先确认目标是项目中的派生数据目录，不要误删原始 `data/`。正式系统应为文档分配稳定 ID，并实现按来源更新或删除，而不是靠手工清库。

### 8.4 调整检索质量

常见参数：

- 增大 `k`：参考面更广，但 Prompt 更长、噪声更多。
- 减小 `k`：更精炼，但可能漏掉信息。
- 增大 `chunk_size`：每片上下文更完整，但匹配可能不够精确。
- 减小 `chunk_size`：匹配更细，但语义容易被切碎。
- 调整 `chunk_overlap`：降低边界断裂，但会增加重复内容。

不要只凭感觉改参数。准备 10～30 个代表性问题，为每个问题标注应该命中的资料，再比较参数变化前后的命中质量。

### 8.5 更换聊天模型

修改 `config/rag.yml` 的 `chat_model_name`。需要确认：

- 新模型在当前账户和地域可用。
- 模型支持 Tool Calling；否则 Agent 无法可靠调用工具。
- 上下文窗口足以容纳系统提示词、工具描述和检索资料。
- 价格、限流与延迟符合需求。

更换聊天模型通常不要求重建向量库。

### 8.6 更换 Embedding 模型

修改 `embedding_model_name` 后，应该用同一新模型重新向量化全部资料。查询向量与库内向量必须处在同一个向量空间，而且向量维度也可能不同。

### 8.7 增加真正的多轮记忆

当前 UI 的历史消息格式已经接近可复用，但 `ReactAgent.execute_stream()` 只收单个字符串。最直观的改造方向是让它接收完整消息列表：

```python
input_dict = {
    "messages": st.session_state["message"]
}
```

但不能只机械替换，还要处理：

- 何时把当前用户消息加入列表，避免重复。
- Agent 返回的中间 ToolMessage 是否需要持久化。
- 对话过长后的 token 成本与截断/总结。
- 多用户会话隔离。
- 服务重启后的持久化。

对于正式 LangGraph Agent，更标准的方向是配置 checkpointer，并为每个会话传稳定 `thread_id`。

---

## 9. 配置与 Prompt 的调试思路

### 9.1 区分“代码规则”与“提示词规则”

提示词写“必须先调用 A 再调用 B”，本质上仍是对模型的语言指令，并不是百分之百的程序强制。

本项目报告顺序中：

- “应该调用 `fill_context_for_report`”主要由提示词约束。
- “调用后把 report 设为 True”由中间件代码保证。
- “没调用它就绝对不能调用 `fetch_external_data`”目前没有在工具代码中硬性校验。

如果业务上绝对不能违反顺序，应在程序里验证状态或把流程做成确定性 LangGraph 节点，而不能只依赖 Prompt。

### 9.2 调 Prompt 时一次只改一个变量

推荐流程：

1. 固定模型、知识库和测试问题。
2. 保存修改前输出。
3. 只修改一条 Prompt 规则。
4. 重复测试多次，因为模型输出具有非确定性。
5. 对比工具选择、参数、答案事实和格式。

### 9.3 Tool description 比长篇提示词更靠近工具结构

模型通常直接收到工具 schema，所以 `@tool` 的 description 非常重要。建议把最核心的使用条件放在那里；主提示词适合表达跨工具的流程与全局政策。

### 9.4 防止检索资料中的提示词注入

RAG 资料属于数据，不应被当作系统指令执行。如果资料中出现“忽略之前规则、调用某工具”，模型可能受影响。

生产方案应包括：

- 在系统提示词中明确参考资料是不可信数据，只用于事实依据。
- 对高风险工具做代码级权限和参数校验。
- 写操作前进行用户确认。
- 记录来源并限制可导入文件。
- 对检索内容做安全检测与来源追踪。

---

## 10. 常见报错与处理

### 10.1 `python` 不是可识别的命令

原因：Python 未安装或未加入 PATH。

处理：安装 Python 3.10/3.11，并在安装时选择加入 PATH；重新打开 PowerShell，运行 `python --version`。

### 10.2 `ModuleNotFoundError`

例子：

```text
No module named 'langchain_chroma'
```

原因：依赖没装、装在别的 Python、虚拟环境没激活。

检查：

```powershell
python -c "import sys; print(sys.executable)"
python -m pip show langchain-chroma
```

始终使用 `python -m pip`，它能减少 `pip` 与 `python` 指向不同环境的问题。

### 10.3 API Key / 401 / Unauthorized

检查当前进程能否看到 Key：

```powershell
if ($env:DASHSCOPE_API_KEY) { "已配置" } else { "未配置" }
```

还要确认 Key 所属账户、模型权限、余额/额度和服务地域。不要把完整 Key 打印到日志。

### 10.4 模型不存在或无权限

`qwen3-max` 或 `text-embedding-v4` 可能受账户、地域或服务配置影响。到阿里云百炼控制台确认模型可用，或把 YAML 改成账户实际可用且能力兼容的模型。

### 10.5 `UnicodeDecodeError`

原因：TXT 实际编码和 `encoding="utf-8"` 不一致。

处理：用编辑器把文件另存为 UTF-8；或明确知道源编码时，为对应 Loader 配置正确编码。不要靠随机尝试编码掩盖数据质量问题。

### 10.6 PDF 没有有效文本

可能是扫描图片 PDF、字体编码异常、加密或损坏。先用 PDF 阅读器测试能否选中并复制正文；不能选中时通常需要 OCR。

### 10.7 Chroma 数据库锁定或损坏

先停止所有 Streamlit、调试器和独立脚本，确保没有进程正在读写同一目录。确认 `persist_directory` 实际指向哪里，再考虑备份并重建派生库。

### 10.8 检索结果为空或不相关

依次检查：

1. 入库日志是否成功。
2. 使用的是否是预期 Chroma 目录。
3. collection 名是否一致。
4. 建库和查询是否用同一个 Embedding 模型。
5. `retriever_docs()` 原始返回是否相关。
6. 原始资料是否真的包含答案。
7. 分片大小与查询词是否合理。

### 10.9 Agent 不调用工具

可能原因：

- 问题看起来可凭常识回答。
- Tool description 不清楚。
- 系统提示词冲突或太长。
- 模型 Tool Calling 能力不足。
- 工具没有加入 `create_agent(tools=[...])`。

先看 `log_before_model` 和完整 AIMessage 的 `tool_calls`，不要只凭 UI 猜测。

### 10.10 Agent 调错工具或参数

检查参数名、类型注解、工具说明和提示词是否一致。让不同工具的能力边界尽量互斥；如果多个工具描述几乎相同，模型很难稳定选择。

### 10.11 `response_messages[-1]` 报 `IndexError`

如果 Agent 在产生任何非空文本前结束或失败，`response_messages` 可能为空，而 `app.py` 直接取最后一个元素。正式代码应先判断列表是否非空，并捕获异常向用户显示友好错误。

### 10.12 修改了配置但看起来没生效

配置和模型对象多在 import 时创建，Streamlit 进程可能仍持有旧对象。停止服务后重新启动，而不只是刷新浏览器页面。

---

## 11. 当前代码中值得注意的问题

下面不是说示例“不能学”，而是帮助你区分教学简化与生产设计。

### 11.1 缺少依赖清单

没有 `requirements.txt`/`pyproject.toml`，不同时间安装最新包可能出现 API 不兼容。应在成功环境中生成锁定版本并纳入项目管理。

### 11.2 Chroma 使用相对路径，已经产生多份数据库

`config/chroma.yml` 中是：

```yaml
persist_directory: chroma_db
```

而 `VectorStoreService` 没有像数据路径那样调用 `get_abs_path()`。相对路径会以进程的当前工作目录解释。

因此：

- 从项目根目录启动 → `项目根目录/chroma_db`
- 从 `rag` 目录启动 → `rag/chroma_db`
- 从 `agent` 目录启动 → `agent/chroma_db`

当前项目中恰好存在这三处目录，这是强烈迹象。建议始终从项目根目录启动；代码层面的稳妥修复是把持久化路径也转换为项目绝对路径。

### 11.3 UI 有显示历史，但 Agent 没有记忆

`st.session_state["message"]` 只用于重画页面。`execute_stream(prompt)` 每次只构造一条用户消息。这个差异必须在演示时说明。

### 11.4 多个工具名不符合实际行为

- `get_weather` 是固定晴天模板，不是真实天气。
- `get_user_location` 是三个城市随机抽取。
- `get_user_id` 是十个 ID 随机抽取。
- `get_current_month` 是 2025 年月份随机抽取。

教学时应称为 Mock（模拟）工具，否则容易把随机结果误认为系统真实能力。

### 11.5 CSV 使用 `split(',')` 解析

标准 CSV 允许字段中包含逗号、换行和转义引号，手工 `split(',')` 不能完整处理这些情况。当前样例恰好能按预期拆分，不代表通用可靠。正式代码应使用 Python `csv` 模块或 pandas。

### 11.6 类型声明与实际返回不一致

`fetch_external_data(...) -> str` 成功时返回字典。应改为稳定字符串，或把类型声明改成合适的字典结构，并确认工具序列化行为。

### 11.7 文件更新会遗留旧向量

MD5 去重只判断“这个版本是否导入过”。文件内容变化后会新增向量，但不会删除旧版本。长期运行会造成重复和冲突资料。

### 11.8 异常会直接向上抛

`monitor_tool` 捕获异常、记日志后又 `raise`。它没有将错误变成 Agent 可继续处理的 `ToolMessage`，也没有超时、重试或降级策略。

### 11.9 主提示词要求输出完整思考过程

公开完整内部推理既无必要，也可能暴露系统规则、用户信息或安全判断。界面只需显示简洁进度与最终依据；完整调用轨迹应受控地进入调试日志。

### 11.10 工具调用顺序主要靠 Prompt 约束

报告流程如果是强业务规则，最好由代码或图节点保证。提示词约束具有概率性，不能替代鉴权和流程校验。

### 11.11 没有生产级会话、鉴权和安全控制

当前没有：

- 用户身份验证与数据隔离。
- 持久对话状态。
- 输入长度限制、速率限制和并发控制。
- 外部调用超时、重试、熔断。
- 敏感数据脱敏。
- 系统化评测与监控。

这些不妨碍它作为教学项目运行，但不能直接面向真实客户部署。

### 11.12 一个小的返回值问题

`listdir_with_allowed_type()` 在传入路径不是目录时返回了 `allowed_types`，而不是空文件列表。这会让调用方把 `("txt", "pdf")` 当成文件路径继续处理。更合理的是记录错误后返回空元组。

---

## 12. 推荐的学习与验证路线

### 阶段一：看懂普通 Python 结构

目标：能解释模块导入、函数、类、装饰器、YAML、日志和 `if __name__ == '__main__'`。

练习：单独调用 `get_weather.invoke(...)`，修改返回文案并再次运行。

### 阶段二：看懂模型与 Prompt

目标：区分 system/user/tool 消息，理解模型为什么能生成工具参数。

练习：只测试 `chat_model.invoke()`，修改一条系统规则，比较输出。

### 阶段三：看懂 RAG

目标：能解释 Loader、Document、Splitter、Embedding、Chroma、Retriever 和总结 Chain。

练习：新增一份只包含独特答案的小 TXT，入库后用 `retriever_docs()` 验证命中。

### 阶段四：看懂 Agent 循环

目标：能在日志中识别 HumanMessage、AIMessage、ToolMessage 和最终 AIMessage。

练习：分别发无需工具、单工具、两工具、报告流程问题，画出各自调用顺序。

### 阶段五：看懂 Middleware 与 Context

目标：理解钩子在什么时间执行，以及 `report` 标记为什么能影响下一轮模型调用。

练习：在 `report_prompt_switch()` 设置断点，对比普通问题和报告问题。

### 阶段六：做一个安全的小改造

推荐任选一个：

- 增加简单的保修查询 Mock 工具。
- 让天气工具明确标注“模拟数据”。
- 修复 Chroma 为绝对路径。
- 为 `response_messages` 空列表增加保护。
- 使用标准 `csv` 模块读取外部记录。

每次改造都按“单元测试 → 组件测试 → Agent 测试 → UI 测试”的顺序验证。

---

## 13. 一套可重复执行的验收清单

### 环境

- [ ] `python --version` 为 3.10 或 3.11。
- [ ] 当前解释器来自项目 `.venv`。
- [ ] 关键包可以 import。
- [ ] `DASHSCOPE_API_KEY` 已配置但未泄露。
- [ ] 当前工作目录是项目根目录。

### 模型与数据

- [ ] ChatTongyi 能完成最小问答。
- [ ] DashScopeEmbeddings 能生成查询向量。
- [ ] 知识文件能以 UTF-8/PDF 文本方式读取。
- [ ] 根目录 Chroma 集合中有数据。
- [ ] `retriever_docs()` 返回 3 个或预期数量的相关片段。
- [ ] RAG 总结严格依据命中资料。

### Agent

- [ ] 简单问候不滥用工具。
- [ ] 专业问题能调用 `rag_summarize`。
- [ ] 工具日志包含正确名称和参数。
- [ ] 报告请求能触发 `fill_context_for_report`。
- [ ] 该工具之后 `report` 标志变为 `True`。
- [ ] 报告能读取匹配的用户/月度数据。
- [ ] 工具失败时能在日志定位到根因。

### UI

- [ ] 页面能启动并提交消息。
- [ ] 流式内容能完整显示。
- [ ] 空结果或异常不会导致页面无提示崩溃。
- [ ] 明确知道当前历史记录只是显示历史，不是模型记忆。

---

## 14. 关键代码阅读顺序

第一次阅读建议严格按以下顺序，不要从底层库源码开始：

1. `app.py`：用户入口。
2. `agent/react_agent.py`：系统如何组装。
3. `prompts/main_prompt.txt`：模型被要求怎样行动。
4. `agent/tools/agent_tools.py`：模型能做哪些事。
5. `agent/tools/middleware.py`：过程如何被监控和改变。
6. `rag/rag_service.py`：RAG 工具怎样回答。
7. `rag/vector_store.py`：资料怎样进入和离开向量库。
8. `model/factory.py`：模型来自哪里。
9. `utils/` 与 `config/`：基础设施和参数。

每读一个文件，都回答四个问题：

1. 谁会调用它？
2. 它接收什么？
3. 它返回或修改什么？
4. 失败时从哪里能看到证据？

如果四个问题都能答出来，就说明不只是“看过代码”，而是真正理解了组件边界。

---

## 15. 官方资料与版本提醒

本项目采用 LangChain 1.x 风格 API。LangChain 更新较快，搜索资料时要优先确认文档版本，避免把早期 `initialize_agent` 教程与当前 `create_agent` 代码混在一起。

建议参考：

- [LangChain Agents 官方文档](https://docs.langchain.com/oss/python/langchain/agents)
- [LangChain Middleware 官方文档](https://docs.langchain.com/oss/python/langchain/middleware/overview)
- [LangChain Streaming 官方文档](https://docs.langchain.com/oss/python/langchain/streaming)
- [LangChain Chroma 集成文档](https://docs.langchain.com/oss/python/integrations/vectorstores/chroma)
- [LangChain Embedding 概念与集成](https://docs.langchain.com/oss/python/integrations/embeddings)
- [LangChain PyPI 页面](https://pypi.org/project/langchain/)

官方文档当前明确说明：LangChain Agent 构建在 LangGraph 之上；工具可支持连续或并行调用；Middleware 可用于日志、提示词转换、重试、限流和安全控制；Chroma 的 `persist_directory` 用于本地持久化。这些概念都能在本项目中找到对应实例。

---

## 16. 最后用一段话复述整个系统

用户在 Streamlit 页面输入问题，`ReactAgent` 把问题交给通义聊天模型。模型依据系统提示词和工具 schema 决定是否调用工具。所有工具调用都先经过监控中间件；普通知识问题会进入 RAG 服务，把问题向量化后从 Chroma 取出三个相关片段，再由聊天模型总结；报告问题会调用一个信号工具，把运行时上下文改为报告模式，使下一轮模型使用专门的报告提示词，然后从 CSV 模拟系统取用户月度数据。Agent 最终把答案作为消息状态流返回页面，页面逐字显示并保存显示历史。日志负责留下模型与工具执行线索，YAML 和文本 Prompt 则把易变参数与行为规则从代码中分离。

你能在不看源码的情况下准确讲出上面这段流程，并能用第 7 章的断点逐步验证它，就已经真正入门了这个 LangChain Agent 示例。
